import QtQuick

// Menu bar
Rectangle {
    id: menuBar
    height: 40
    color: "black"
    // positioned at the top
    anchors.top: parent.top
    anchors.left: parent.left
    anchors.right: parent.right
}
